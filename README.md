# Historical CVE Fetch

This repository allows you to fetch historical CVE (Common Vulnerabilities and Exposures) data.

## Usage

1. Take the `vuln-analyzer.jar` from the repo

2. Put it inside a maven project and run it
    ```sh
    java -jar vuln-analyzer.jar
    ```

The fetched cumulative CVE data from all dependencies will be printed to console by year

